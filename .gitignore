# Secrets
.env
.env.local
.env.*.local

# App data (keep the dir itself so Docker doesn't create it as root)
app/data/*
!app/data/.gitkeep

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.python-version
dist/
.ruff_cache/

# uv
# (uv.lock is committed for reproducibility)

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Docker
docker-compose.override.yml
